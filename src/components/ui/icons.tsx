import { Moon, PartyPopper, Send, Sun, type LucideIcon } from "lucide-react";

export type Icon = LucideIcon;

export const Icons = {
  sun: Sun,
  moon: Moon,
  partypopper: PartyPopper,
  send: Send,
};
